spring:
  application:
    name: drk-fls
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri: 'http://localhost:8080'
      username: 'config-server'
      password: 'password'
      label: v1.0
  profiles:
    active:
      - uat