import {Districts} from './Districts';
import {Alias} from './Alias';

export class DistrictAlias {

    Districts: Districts[];

    content: Alias[];
    totalPages: number;
    totalElements: number;
    last: boolean;
    size: number;
    first: boolean;
    sort: string;
    numberOfElements: number;
}
