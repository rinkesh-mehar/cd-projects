spring:
  application:
    name: basic-farmer-app
  cloud:
    config:
      uri: ${config.url}
      username: ${config.username}
      password: ${config.password}
      label: v1.0
  profiles:
    active:
      - ${active.profile}
